import React from 'react';
import CartShareContainer from '../../containers/cartshare/CartShareContainer';

const CartSharePage = () => {
    return <CartShareContainer />;
};

export default CartSharePage;
